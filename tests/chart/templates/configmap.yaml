apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "myorg.fullname" . }}
  labels:
    {{- include "myorg.labels" . | nindent 4 }}
data:
  name: {{ include "myorg.name" . | quote }}
  fullname: {{ include "myorg.fullname" . | quote }}
  chart: {{ include "myorg.chart" . | quote }}
