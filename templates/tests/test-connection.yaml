apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plat-eng-commons-package.fullname" . }}-test-connection"
  labels:
    {{- include "plat-eng-commons-package.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plat-eng-commons-package.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
